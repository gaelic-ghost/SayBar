# TODO

- Add a runtime-on verification pass for embedded launch and graceful `land()` shutdown once we have a stable non-template test path that can exercise the full embedded runtime without making every UI test heavy.
- Revisit whether SayBar should adopt `SpeakSwiftlyServer`'s standalone-install and retained-log helper APIs when the product intentionally grows an app-managed standalone-server mode.
